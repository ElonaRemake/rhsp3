rhsp3 examples
==============

This directory contains various examples of the use of rhsp3. As a general listing of what you can find in this
directory:

* `exrand` is a reimplementation of an existing HSP library by D.N.A.Softwares in Rust. The directory contains the
  original readme and the original sample program, alongside `exrand.hpi` and `exrand.as` which contain the Rust
  reimplementation.
